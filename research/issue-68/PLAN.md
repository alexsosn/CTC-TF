# #68 Plan — native Burns entities and scoped archaeology

## Completed research and TDD gates (synthetic evidence only)

1. **Research:** `RESEARCH.md` inventories Burns's nine workbook classifications and the separate worksheet role/semantic-status axes. CUC has no `entity` or `fragment` nodes. The existing feature-only module cannot express overlapping Burns occurrences as distinct nodes. `headword` is a source label, not a linguistically verified `lemma`.
2. **RED → GREEN, word-feature fallback:** `tests/test_native_burns_features.py` was RED on missing `native_features.py` in [CI run 35132321012](https://github.com/alexsosn/CTC-TF/actions/runs/35132321012); the pure helper then passed [CI 35132443796](https://github.com/alexsosn/CTC-TF/actions/runs/35132443796). It exports native nine-category word predicates and scalar single-word labels, recording colliding values separately. This *cannot* represent overlapping phrase and headword identities and must not substitute for entity nodes.
3. **RED → GREEN, actual entities:** `tests/test_burns_entity_extension.py` went RED before `entity_extension.py` existed in [CI 35132735472](https://github.com/alexsosn/CTC-TF/actions/runs/35132735472). Its synthetic CUC proof saves an *extended, complete* `otype`/`oslots` warp with appended Burns `entity` nodes, retaining all original node IDs/sign slots. Real `Fabric` reload, native `S.search` and `L.d` demonstrate separate entities on the same word and overlapping multiword phrases. Root and two workbook category searches and an explicitly excluded homograph are independently exercised in `test_burns_entity_query_matrix.py`.
4. **Independent adversarial review of prototype:** [PR #69 review](https://github.com/alexsosn/CTC-TF/pull/69) identified per-word giant temporary slot allocations and insufficient API/index identity checks. New adversarial mismatch tests and bounded slot checks address those findings; [CI 35133471036](https://github.com/alexsosn/CTC-TF/actions/runs/35133471036) passed the complete five-job matrix at that intermediate head. The separate API/index check is not equivalent to the on-disk reviewed CUC fingerprint; publisher must retain that gate.
5. **RED → GREEN, tablet fields:** `tests/test_burns_tablet_findspots.py` went RED on missing `tablet_findspots.py` in [CI 35133684230](https://github.com/alexsosn/CTC-TF/actions/runs/35133684230). `derive_tablet_findspots` publishes a single CUC-tablet scalar *only when every mapped source row has the same nonblank value*, and records conflicts, incompleteness and unmapped row IDs outside the public features. `tests/test_burns_entity_findspots.py` went RED before integration; synthetic native TF search `tablet burns_locus=GP` subsequently passed. No location is attached to word or entity nodes. Explicit CUC fragment mapping and Burns Appendix validation are not available; no fragment properties are asserted.

## Architecture decision (prototype, not released)

A true occurrence requires one TF `entity` node per unambiguously aligned Burns occurrence, with source headword, root, source-defined thematic category, worksheet role, semantic status and section as **scalar TF features**. Its `oslots` covers the exact CUC word-span sign slots. Original CUC nodes retain IDs and sign-slot coordinates, but `otype`/`oslots` must be replaced by a coherent extended warp, not presented as an ordinary feature-only CUC overlay. Status `homograph_excluded` is queryable on its own entity: **a category-only query means Burns workbook membership, not a positive assertion; filter `burns_semantic_status` for positive-only research**. No `burns_lemma` is fabricated. Structural-only/ambiguous/out-of-CUC/nontextual occurrences stay accounted for in the sidecar without invented word hits.

## Remaining integration and release blockers, in order

1. **Provenance and compatibility:** inspect the real reviewed CUC `tf/0.2.8`, map source→CUC/fragment IDs, audit real collision and coverage counts plus actual nine directory labels. Ensure extended warp source is bound to the exact on-disk SHA-256 reviewed dataset and refuses mismatched `oslots`, not merely equal node counts and word transcriptions. No source CSVs or full CUC are available in this execution; synthetic passing tests are not a real-data audit.
2. **Versioned v2 user path:** design RED tests for the actual `module` CLI output; migrate CLI, emitter, full schema/feature inventory, report, deterministic validation and publisher *together*, retiring six repeated JSON features and storing full source/alignment only in the local sidecar. `otype`/`oslots` become explicitly owned extension features. Preserve existing output on failed validation/TF.save/replace and never delete unknown user-owned TF files. Coordinate exact-inventory checks with pending PR #65 and release #16.
3. **Complete semantics:** assert all nine workbook classes, exact single-/multiword and overlapping entity identity; distinguish `headword` from verified lemma; preserve negative homograph status; native TF lexical/root/category query matrix; explicitly count unselected and source-only occurrences. Reject silent single-value overwrite, JSON arrays disguised as scalar features, and false word-level assertions. Archaeological disagreements and missing observations must remain sidecar entries; Appendix/fragment behavior must be evidence-based.
4. **Consumer/acceptance:** run installed-package tests, Python 3.10/3.12/3.13, *new* native extended-warp composition against reviewed CUC and cfabric-mcp, Agora materializer consumer contract where applicable, publisher rollback and foreign-file safety. Existing pinned Agora/Context-Fabric CI jobs still exercise their old contract and do not prove support for overriding warp. Audit real output disk/memory implications.
5. **Independent review at exact final head:** genuinely adversarial pass on complete v2 code, output ownership, CUC identity and query correctness, address findings, rerun full CI; only then consider undrafting/merging #69 and unblocking release #16. Until then, master remains unchanged and #68 open.

## Stop conditions

Do not release this prototype, merge PR #69, alter frozen master or mark #68 fixed if source coverage is unverified, native queries fail, conflicting findspots are silently collapsed, original CUC identity can drift, extended warp breaks consumers, or publication can delete foreign features. Test success for *isolated builders* is insufficient to assert the current `module` command is repaired.
